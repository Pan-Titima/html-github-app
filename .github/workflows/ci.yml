name: GitHub Actions Demo

on:
  push:
    branches: 
      - main 
  pull_request:
    branches:
      - '*feature'
jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World" 